//  FUNCTIONS01-005: Write a function that takes a number as an argument and returns the sum of its digits.

function sumOfNumber(a){
var numConvert = a.toString().split(' ');
let sum = 0;
    for (let i = 0; i<numConvert.length;i++){
         numConvert[i];
         console.log(numConvert[i]);

    } 

} console.log(sumOfNumber(2445));