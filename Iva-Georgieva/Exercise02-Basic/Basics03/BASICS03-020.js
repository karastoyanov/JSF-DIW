/* Declare a variable. Assign an integer number. Print on the console the 
variable, the binary, octal and hexadecimal representation. */

let integerNumber;

integerNumber = 89;

console.log(integerNumber, integerNumber.toString(2), integerNumber.toString(8), integerNumber.toString(16));
