/*  Define a string variable with following format:
My name’s <name>. I’m <age> years old.
Previously define the name and the age values. They will not be changed during the 
further code. */

const myName = "Iva Georgieva";
const myAge = 30;

let sentence = `My name's ${myName}. I'm ${myAge} years old.`;
console.log(sentence);
