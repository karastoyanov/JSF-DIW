/*  Using Number and BigInt types, define variables and perform the actions 
with each of the arithmetic operations ( + - * / ). Save the results in diWerent variables 
and print them on the console. */

let typeNumber = 7;
let typeBigInt = 67n;

let arOper1 = typeNumber + Number(typeBigInt);
let arOper2 = typeNumber * Number(typeBigInt);
let arOper3 = typeNumber / Number(typeBigInt);
let arOper4 = typeNumber - Number(typeBigInt);

console.log(arOper1);
console.log(arOper2);
console.log(arOper3);
console.log(arOper4);