/*BASICS03-007: Declare a constant and assign one digit. Print on one line the constant, the 
power of two (N2
), the power of three (N3
) on the console */
const NUMBER = 10;

console.log(`Constant -> ${NUMBER}, Power of two -> ${Math.pow(NUMBER,2)}, Power of three -> ${Math.pow(NUMBER,3)}`)
