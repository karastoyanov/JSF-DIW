/* Declare five variables. Assign them with the randomly chosen emoji 
characters. Print the UNICODE codes in hex format on the console – on different lines. */
let tearsOfJoy = "😂";
console.log(tearsOfJoy.codePointAt(0).toString(16));

let winkingFace = "😉";
console.log(winkingFace.codePointAt(0).toString(16));

let angelFace = "😇";
console.log(angelFace.codePointAt(0).toString(16));

let inLove = "🥰";
console.log(inLove.codePointAt(0).toString(16));

let moneyFace = "🤑";
console.log(moneyFace.codePointAt(0).toString(16));
