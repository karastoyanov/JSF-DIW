/* Declare five variables. Assign them with the randomly chosen emoji 
characters. Print the UNICODE codes in decimal format on the console – on different 
lines. */

let tearsOfJoy = "😂";
console.log(tearsOfJoy.codePointAt(0).toString(10));

let winkingFace = "😉";
console.log(winkingFace.codePointAt(0).toString(10));

let angelFace = "😇";
console.log(angelFace.codePointAt(0).toString(10));

let inLove = "🥰";
console.log(inLove.codePointAt(0).toString(10));

let moneyFace = "🤑";
console.log(moneyFace.codePointAt(0).toString(10));
