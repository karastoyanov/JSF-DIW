/* Write a JavaScript program to convert degrees in radians. Print on the console 
an appropriate message. */

let d = 150;
let pi = 3.1416;
let r = d * (pi / 180);
console.log(`${d} degrees is ${r.toFixed(4)} radians.`)
