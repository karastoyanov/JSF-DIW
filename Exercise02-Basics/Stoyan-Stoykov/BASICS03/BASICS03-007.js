/**
 * BASICS03-007: Declare a constant and assign one digit. Print on one line the constant, the 
    power of two (N^2), the power of three (N^3) on the console.
 */

const d = 3;
console.log(`N=${d}, N^2=${d**2}, N^3=${d**3}`);