/*
BASICS03-020: Declare a variable. Assign an integer number. Print on the console the 
variable, the binary, octal and hexadecimal representation.
*/

let a = 12;

console.log(a.toString(2), a.toString(8),a.toString(16));