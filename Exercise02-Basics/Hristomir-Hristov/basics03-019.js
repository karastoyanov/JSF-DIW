// Write a JavaScript program to convert km/h into m/s. Print on the console an appropriate message.


let speedInHs = 160;
let speedInMs = speedInHs * (1000 / 3600);

console.log(speedInHs + " km per hour equal to " + speedInMs + " meters per second.");