// Write a JavaScript program to convert degrees in radians.
// Print on the console an appropriate message.

let degrees = 90;
let pi = Math.PI;

let radians = degrees * (pi / 180);

console.log(`${degrees} degrees are equal to ${radians} radians.`);