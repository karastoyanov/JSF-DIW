//Write a JavaScript program to convert km/h into m/s.
// Print on the console an appropriate message.

let x = 110; // km/h
let y = x * 1000 / 3600; // m/s

console.log(`The speed of ${x} km/h is equal to ${y.toFixed(2)} m/s`);