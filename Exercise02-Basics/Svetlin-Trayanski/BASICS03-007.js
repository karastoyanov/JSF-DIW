/*Declare a constant and assign one digit. Print on one line the constant, the 
power of two (N2), the power of three (N3) on the console. */

const digit = 2;
console.log(`${digit}\n${Math.pow(digit, 2)}\n${Math.pow(digit, 3)}`) 