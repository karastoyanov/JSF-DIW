/*
BASICS03-006
Declare one variable, assign integer number.
Check if the variable contains an even number.
Print on the console appropriate message.
*/

let num = 8;

let message = (num % 2 === 0) ? "The number is even." : "The number is odd.";

console.log(message);