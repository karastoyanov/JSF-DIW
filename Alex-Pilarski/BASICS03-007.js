// Declare a constant and assign one digit. Print on one line the constant, the
// power of two (N2), the power of three (N3) on the console.


// Declare a constant and assign one digit
const num = 5;

// Calculate the power of two and three
const powerOfTwo = Math.pow(num, 2);
const powerOfThree = Math.pow(num, 3);

// Print the constant, the power of two, and the power of three on one line
console.log(`Constant: ${num}, Power of Two: ${powerOfTwo}, Power of Three: ${powerOfThree}`);
