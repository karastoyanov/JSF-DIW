// Write a JavaScript program to convert km/h into m/s. Print on the console an appropriate message.

let speedKmPerHour = 80;
let speedMPerSec = speedKmPerHour * (1000 / 3600);

console.log(speedKmPerHour + " km/h is equal to " + speedMPerSec.toFixed(2) + " m/s.");
