//      DTYPES01-005: Define a string variable with following format: My name’s <name>. I’m <age> years old. 
//      Previously define the name and the age values. They will not be changed during the further code.

let myName = 'Plamen Dilkinski';
let myAge  = "33";

resultVar = `My name\'s ${myName}. I\'m ${myAge} years old.`

console.log(resultVar)