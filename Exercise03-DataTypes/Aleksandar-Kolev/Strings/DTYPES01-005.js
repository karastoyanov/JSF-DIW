//  Define a string variable with following format:
// My name’s <name>. I’m <age> years old.
// Previously define the name and the age values. They will not be changed during the further code.

let myname = "Aleksandar";
let age = "22";

let string = `My name's ${myname}. I'm ${age} years old.`;

console.log(string);