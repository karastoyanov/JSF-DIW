/* Write a program in JavaScript to check if a given number is even or odd.
// use a ternary operator and string template */

let number = 7;

let checkup = (number % 2 === 0) ? `The ${number} is even.` : `The ${number} is odd.`;

console.log(checkup);
